<Window x:Class="ScreenCaptureApp.App.GifRecordingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Recording animated GIF"
        Width="390" Height="142" SizeToContent="Manual"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        Topmost="True" ShowActivated="False" Background="Transparent"
        AllowsTransparency="True" WindowStartupLocation="CenterScreen">
  <Border Margin="10" Padding="16" Background="#F5212328" BorderBrush="#5AFFFFFF"
          BorderThickness="1" CornerRadius="12">
    <Border.Effect>
      <DropShadowEffect BlurRadius="18" ShadowDepth="3" Opacity="0.45" />
    </Border.Effect>
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <Ellipse Width="13" Height="13" Fill="#FF4D4F" VerticalAlignment="Center" />
      <StackPanel Grid.Column="1" Margin="12,0">
        <TextBlock Text="Recording animated GIF" Foreground="White" FontSize="15" FontWeight="SemiBold" />
        <TextBlock x:Name="StatusText" Text="Starting…" Foreground="#BFFFFFFF" Margin="0,4,0,0" />
      </StackPanel>
      <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
        <Button Content="Stop" Click="Stop_Click" AutomationProperties.Name="Stop and save recording" />
        <Button Content="Cancel" Click="Cancel_Click" AutomationProperties.Name="Cancel recording" />
      </StackPanel>
    </Grid>
  </Border>
</Window>
